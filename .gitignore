# OS
.DS_Store

# Claude Code local config
.claude/

.vscode/

# Nested git repo — managed separately at github.com/Nacar11/asima-backend
asima-backend/
asima-frontend/
tasks/

# Local-only seed assets (e.g. the mock attachment placeholder). Regenerated
# locally — see the demo-seed-data plan. Not committed (binary, dev-only).
public/
